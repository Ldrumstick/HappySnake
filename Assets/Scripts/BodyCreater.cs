using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BodyCreater : MonoBehaviour {
    /*
    public GameObject body;
    private Head_Move head;
    private Vector3 createpos = Vector3.zero;
    

    void Start () {
        head = GetComponent<Head_Move>();
	}
	
	void Update () {
        
        if (Input.GetMouseButtonDown(0))
        {
            //Instantiate(body, head.oldPosition, Quaternion.identity);
            Instantiate(body,head.oldPosition,Quaternion.identity);

        }
	}*/
}
